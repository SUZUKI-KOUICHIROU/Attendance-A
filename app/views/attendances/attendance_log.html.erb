<% provide(:title, 'attendance-log') %>

<h1>勤怠ログ</h1>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-striped table-bordered table-condensed">
      <%= link_to "リセット", '#', class: "btn btn-lg btn-default" %> 
      <br>
        <%= form_with(url: attendances_attendance_log_user_path, local: true) do |f| %>   
          <%= f.date_select :arrival_date, {}, class: 'form-control', id: "date-select" %>
      <br> 
      <% end %>

    <thead> 
      <tr>
        <th>日付</th>
        <th>変更前出社時間</th>
        <th>変更前退社時間</th>
        <th>変更後出社</th>
        <th>変更後退社</th>
        <th>指示者</th>
        <th>承認日</th>
      </tr>
    </thead>
    <tbody>
    <!--% @bases.each do |base| %-->     
      <tr>
      <!--%= form_with(model: base, url: basis_path(base), local: true, method: :patch) do |f| %-->
        <th>2020-09-03</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
       <!--% end %-->
     <!--% end %-->
    </tbody>
  </table>
</div>